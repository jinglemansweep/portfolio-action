# Favicon & Site Logo

Add favicon support and a small site logo image displayed to the left of the site name in the header/nav bar.
Ideally we should use the main site logo image if set otherwise something generic
